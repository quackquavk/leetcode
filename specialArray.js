var specialArray = function (nums) {
  nums.filter((num) => num !== 0);
  nums.sort();
  let specialNum=0;
  for (let i = 0; i < nums.length; i++) {
        if(nums[i] >= specialNum){
            
        }
  }
};
console.log(specialArray([1, 0, 4, 0]));
